# Integrations

External provider adapters belong here.

Use this folder for projects named `TerraLink.Integrations.<Provider>` when a provider SDK, HTTP API, queue, storage service, email service, or similar external boundary is introduced. Domain or a UseCase should define the abstraction; integration projects implement it.
