namespace TerraLink.UseCase.Asset.Features.Lands.Endpoints;

public sealed partial class DeleteLandEndpoint
{
    public sealed class Response
    {
        public static Response Default { get; } = new();
    }
}