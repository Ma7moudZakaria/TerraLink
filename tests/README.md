# Tests

Use this folder for test projects:

- `TerraLink.UnitTests`
- `TerraLink.IntegrationTests`

Unit tests should cover domain rules, value objects, operation handlers, validators, and specifications. Integration tests should cover API behavior, persistence mappings, migrations, and provider boundaries.
